import type { BoardKeymapSource } from '../types'

export const keymapSource: BoardKeymapSource = {
  keymapRelative: 'config/dax3.keymap',
  jsonRelative: 'config/dax3.json',
  envVarName: 'DAX3_REPO_ROOT',
  defaultLayerName: 'default_layer',
}
