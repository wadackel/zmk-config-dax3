export DIRENV_LOG_FORMAT=$'\033[2mdirenv: %s\033[0m'
watch_file flake.nix
watch_file flake.lock

# Apple Silicon native試行（失敗時はコメント解除）
use flake
# use flake . --system x86_64-darwin

echo "ZMK development environment activated"
